package com.aixu.meeting.utils;

import java.util.Comparator;

import com.aixu.meeting.domain.AixuMeeting;

/** 
* @author Jianglinle
* @date 2017年11月14日 上午10:19:14 
* @version 1.0.0
*
*/
public class AixuMeetingComparator implements Comparator<AixuMeeting>{
	 public int compare (AixuMeeting left,AixuMeeting right) { 
	        if (right.getStaTime().getTime() > left.getStaTime().getTime()) {
				return 1;
			} else {
				return 0;
			}
	    } 
}
 